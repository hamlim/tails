import type { Metadata } from "next";

export let metadata: Metadata = {
  title: "Login to Tails"
}

export default function LoginPage() {
  return (
    <div>
      <h1>Login to Tails</h1>
      <p>Login to Tails</p>
    </div>
  )
}
